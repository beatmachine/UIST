<UserControl x:Class="SnakeGame.ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SnakeGame"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Name="Up" Content="Up" Grid.Row="0" Grid.Column="1"/>
        <Button Name="Down" Content="Down" Grid.Row="1" Grid.Column="1"/>
        <Button Name="Left" Content="Left" Grid.Row="1" Grid.Column="0"/>
        <Button Name="Right" Content="Right" Grid.Row="1" Grid.Column="2"/>
        <Button Name="Play" Content="Play" Grid.Row="0" Grid.Column="0"/>
        <Button Name="Pause" Content="Pause" Grid.Row="0" Grid.Column="2"/>
    </Grid>
</UserControl>
